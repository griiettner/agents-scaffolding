---
name: tkt-report
description: "Legacy alias; reporting is handled by tkt-exec or tkt-update. Use only when user explicitly invokes /tkt-report."
disable-model-invocation: true
---

# /tkt-report

This command is deprecated as a standalone workflow.

Use:

- `/tkt-exec` for completion reporting after execution.
- `/tkt-update` for later ticket-specific fixes, comments, or regressions.

Do not create `report.md` as a placeholder.
